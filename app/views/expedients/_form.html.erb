<%= form_with(model: expedient, local: true) do |form| %>
  <% if expedient.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(expedient.errors.count, "error") %> prohibited this expedient from being saved:</h2>

      <ul>
      <% expedient.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :expediente %>
    <%= form.text_field :expediente, id: :expedient_expediente %>
  </div>

  <div class="field">
    <%= form.label :hojas %>
    <%= form.number_field :hojas, id: :expedient_hojas %>
  </div>

  <div class="field">
    <%= form.label :a_inicial %>
    <%= form.number_field :a_inicial, id: :expedient_a_inicial %>
  </div>

  <div class="field">
    <%= form.label :a_final %>
    <%= form.number_field :a_final, id: :expedient_a_final %>
  </div>

  <div class="field">
    <%= form.label :hojas %>
    <%= form.number_field :hojas, id: :expedient_hojas %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
